var x = 10;
var y = 5;
var isEqual = x === y; //This will return false since the numbers are not equal.
var isNotEqual = x !== y; //This will return true since they are not equal in value.
var isGreaterThan = x > y; //This will return true since x has a greater value than y.
var isLessThan = x < y; //This will results in false since x has a great number than y.
var isGreaterOrEqual = x >= y; //True, it's greater but not equal.
var isLessOrEqual = x <= y; //False
console.log(isLessOrEqual);
